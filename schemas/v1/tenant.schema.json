{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://olon.js.org/schemas/v1/tenant.schema.json",
  "title": "Olon Tenant Manifest",
  "description": "Top-level Olon tenant manifest. Bundles a tenant's complete public contract: brand identity, design system tokens, site shell, navigation, and full page list. Each top-level field references a separately-published canonical schema; this manifest is the entry point for agents discovering or validating a complete Olon tenant.",
  "type": "object",
  "required": [
    "identity",
    "design",
    "site",
    "menu",
    "pages"
  ],
  "properties": {
    "design": {
      "$ref": "design.schema.json"
    },
    "identity": {
      "$ref": "#/definitions/SiteIdentity"
    },
    "menu": {
      "$ref": "menu.schema.json"
    },
    "pages": {
      "type": "array",
      "items": {
        "$ref": "page.schema.json"
      }
    },
    "site": {
      "$ref": "site.schema.json"
    }
  },
  "additionalProperties": false,
  "definitions": {
    "SiteIdentity": {
      "description": "Brand identity for a site: human-readable name and logo URL. Used by themes for the document title, header brand, social cards, and PWA manifests.",
      "type": "object",
      "required": [
        "title"
      ],
      "properties": {
        "title": {
          "description": "Human-readable site name. Used in page titles, social previews, and the default header brand.",
          "type": "string"
        },
        "logoUrl": {
          "description": "URL of the site logo, root-relative (`/brand/logo.svg`) or absolute. SVG is preferred for resolution independence; the theme decides aspect-ratio handling (wordmark vs. square mark).",
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  },
  "examples": [
    {
      "design": {
        "name": "Olon",
        "tokens": {
          "colors": {
            "background": "hsl(215 28% 7%)",
            "foreground": "hsl(214 33% 84%)",
            "primary": "hsl(222 100% 54%)"
          }
        }
      },
      "identity": {
        "title": "Olon",
        "logoUrl": "/brand/mark/olon-mark-dark.svg"
      },
      "menu": {
        "main": [
          {
            "href": "/",
            "label": "Home"
          },
          {
            "href": "/docs",
            "label": "Docs"
          }
        ]
      },
      "pages": [
        {
          "id": "home-page",
          "meta": {
            "title": "OlonJS — the contract layer for the agentic web",
            "description": "OlonJS is the contract layer for the agentic web. A typed, deterministic JSON contract that AI agents and humans both understand."
          },
          "sections": [],
          "slug": "home"
        }
      ],
      "site": {
        "footer": {
          "type": "footer",
          "data": {
            "copyright": "© 2026 OlonJS"
          },
          "id": "global-footer"
        },
        "identity": {
          "title": "Olon"
        }
      }
    }
  ]
}
