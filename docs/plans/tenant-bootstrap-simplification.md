# Plan: DNA bootstrap simplification (`useTenantBootstrap`)

Repo: `npm-jpcore` · Scope: DNA only · Skill: code-simplification  
Status: **Done** (`tsc` green)

## Slices

1. Normalize blank lines — done (691 → 322)
2. Extract `bootLocal` / `bootStatic` / `bootLive` — done
3. Verify `tsc` — done (exit 0)

## Out of scope

- Promote to `@olonjs/react`
- Change platform contracts / SPP clients
